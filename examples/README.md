# monapaのサンプルプログラム集


## `cfg1.rs`

パーサの最も単純な機能として、monapaはある文字列が指定の形式文法を満たしているかどうかをYes/Noで判定することができます。
指摘できる文法は、正確には解析表現文法(PEG)ですが、以下ではあまり細部にこだわらずに書いていきます。

```
S -> aSb | ε
```

という文法を判定するサンプルが`cfg1.rs`です。
`ab` `aabb`などの文字列は受理され、`abb` `ac`などは拒否されます。
